# A trivial fixture used by dmut/run_test.go's integration test — proving
# rel's own wiring (Run -> dmutlib.ReadAndRunMutations, Output redirection)
# works, not re-testing dmut's own engine.

widgets:
  sql:
    - |
      create table widgets (
        id int primary key,
        name text not null
      );
