# yaml-embedded-languages: sql

hotel.staff:
  needs: [hotel.properties]
  sql:
    - |
      create table hotel.staff (
        id bigint generated always as identity primary key,
        property_id bigint not null references hotel.properties (id),
        manager_id bigint references hotel.staff (id),
        name text not null,
        role text not null
      );

  meta:
    - up: |
        comment on table hotel.staff is 'Property staff, self-referencing via manager_id - kept as the fixture''s instance of the self-join case from querying.md''s own worked example.';
      down:
